'use strict';

let isMonday = false;
const isMorning = true;

if(isMorning) isOnday = true;   // the typo error would be rectified using strict

if(isMonday) console.log("It is Monday, let's hit the gym");

//if strict is absent, then the code would not run and would not display any error
